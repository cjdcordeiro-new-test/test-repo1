name: 'kamehameha'
description: 'just some test'
author: 'Cris'
inputs:
  token:
    required: true
  working-directory:
    description: fadfdsfsdfdsf
    required: false
runs:
  using: 'composite'
  steps: 
    - run: ls && pwd
      shell: bash
    - run: cat foo/r/test/run-test.sh
      shell: bash
    - run: chmod +x foo/r/test/run-test.sh
      shell: bash
    - run: ./foo/r/test/run-test.sh
      shell: bash
