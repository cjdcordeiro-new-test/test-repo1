name: 'kamehameha'
description: 'just some test'
author: 'Cris'
inputs:
  token:
    required: true
  working-directory:
    description: fadfdsfsdfdsf
    required: false
runs:
  using: 'composite'
  steps: 
    - run: cat run-test.sh
      shell: bash
    - run: chmod +x run-test.sh
      shell: bash
    - run: ./run-test.sh
      shell: bash
